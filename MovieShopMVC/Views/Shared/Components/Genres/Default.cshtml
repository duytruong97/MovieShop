@model List<ApplicationCore.Models.GenreModel>;

	@foreach(var genre in Model){
		<a asp-controller="MovieController" asp-action="genre" asp-route-id ="@genre.Id" class="dropdown-item">
			@genre.Name
		</a>
	} 